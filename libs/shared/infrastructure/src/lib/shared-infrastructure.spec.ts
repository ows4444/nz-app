import { sharedInfrastructure } from './shared-infrastructure';

describe('sharedInfrastructure', () => {
  it('should work', () => {
    expect(sharedInfrastructure()).toEqual('shared-infrastructure');
  });
});
