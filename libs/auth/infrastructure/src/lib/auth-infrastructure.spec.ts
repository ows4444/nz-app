import { authInfrastructure } from './auth-infrastructure';

describe('authInfrastructure', () => {
  it('should work', () => {
    expect(authInfrastructure()).toEqual('auth-infrastructure');
  });
});
